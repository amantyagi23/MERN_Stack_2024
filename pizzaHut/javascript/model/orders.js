export class Order{

    constructor(orderId,userId,pizzaId,username,count){
        this.orderId = orderId,
        this.userId = userId,
        this.pizzaId = pizzaId,
        this.username = username,
        this.count = count
    }
}